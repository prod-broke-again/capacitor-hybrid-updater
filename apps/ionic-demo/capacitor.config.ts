import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hybrid.updater.demo',
  appName: 'Hybrid Updater Demo',
  webDir: 'dist'
};

export default config;
