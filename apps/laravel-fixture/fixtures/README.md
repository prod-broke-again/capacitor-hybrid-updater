# Smoke test files

- `bundle-1.0.1.zip` — generated by `scripts/create-fixtures.ps1` (ZIP with root `index.html`, required by the package validator).
- APK uploads: Laravel validates MIME type `apk`. Use a real build artifact path via `APK_PATH` when running `scripts/smoke-checks.sh`, or test APK upload manually from your CI/device build output.
